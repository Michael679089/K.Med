<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".QRCodeScanTestActivity">

    <!-- Barcode Scanner View (Always Visible) -->

    <!-- Toggle Scanner Button -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="183dp"
        android:layout_margin="10dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/toggleLightBTN"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="toggle light" />

        <com.journeyapps.barcodescanner.BarcodeView
            android:id="@+id/scanner"
            android:layout_width="175dp"
            android:layout_height="158dp"
            android:layout_margin="10dp"
            android:padding="0sp" />

        <Button
            android:id="@+id/toggleAutoFocusBTN"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="toggle autofocus" />
    </LinearLayout>

    <Button
        android:id="@+id/toggleScannerButton"
        android:layout_width="206dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="0sp"
        android:layout_marginStart="145dp"
        android:layout_marginTop="252dp"
        android:layout_marginEnd="131dp"
        android:padding="0sp"
        android:text="Start Scanner" />

    <EditText
        android:id="@+id/PIDFromScannerET"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:ems="10"
        android:hint="Or enter PID Here"
        android:inputType="text"
        android:textAlignment="center" />

    <Button
        android:id="@+id/toggleScannerButton2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="0sp"
        android:layout_marginStart="145dp"
        android:layout_marginTop="252dp"
        android:layout_marginEnd="131dp"
        android:padding="0sp"
        android:text="Submit" />

</LinearLayout>
